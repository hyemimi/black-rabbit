//import { instance } from "@/libs/api/client";
import { useMutation } from "@tanstack/react-query";

/* 리뷰를 삭제합니다 */

/* export default function useAddAddressMutation() {
  return useMutation((reviewId: number) =>
    instance.post<any>(`/reviews/${reviewId}`)
  );
} */
