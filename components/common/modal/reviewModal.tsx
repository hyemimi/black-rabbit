import styled from "styled-components";
import { ModalDiv } from "./ModalDiv";

export default function ReviewModal() {
  // const { scrollY } = useScroll();
  //console.log(scrollY.get());
  return <ModalDiv> 모달임 </ModalDiv>;
}
