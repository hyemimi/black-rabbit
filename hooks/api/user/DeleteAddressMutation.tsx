//import { instance } from "@/libs/api/client";
import { useMutation } from "@tanstack/react-query";

/* 주소지를 삭제합니다 */
/* export default function useDeleteAddressMutation() {
  return useMutation((addressId: number) =>
    instance.post(`/user/address/delete`, null, {
      params: {
        addressId,
      },
    })
  );
} */
